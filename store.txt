<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MoviPilot - Slideshow Card</title>
  
  <style>
    /* 1. Page Background Setup */
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: #1a1a1a; /* Dark sleek background */
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    /* 2. The Main Card Container */
    .showcase-card {
      position: relative;
      width: 450px;
      height: 600px;
      border-radius: 20px;
      overflow: hidden; /* Clips the sliding images safely inside the card corners */
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.6);
    }

    /* 3. Base Slide Settings */
    .card-slide {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-size: cover;
      background-position: center;
      opacity: 0; /* Hidden until its timeline trigger opens */
      
      /* Total slideshow loop takes 12 seconds, repeats infinitely */
      animation: infiniteCardSlider 12s infinite ease-in-out;
    }

    /* 4. Assigning Different Images & Staggered Delay Timers */
    .img-1 {
      background-image: url('https://picsum.photos');
      animation-delay: 0s;
    }

    .img-2 {
      background-image: url('https://picsum.photos');
      animation-delay: 4s; /* Starts 4 seconds down the timeline */
    }

    .img-3 {
      background-image: url('https://picsum.photos');
      animation-delay: 8s; /* Starts 8 seconds down the timeline */
    }

    /* 5. Protective Overlay for Text Readability */
    .card-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      /* Dark gradient shading coming up from the card bottom */
      background: linear-gradient(to top, rgba(0, 0, 0, 0.85) 0%, rgba(0, 0, 0, 0.3) 60%);
      z-index: 2;
    }

    /* 6. Foreground Card Text Structure */
    .card-content {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 100%;
      padding: 35px;
      box-sizing: border-box;
      z-index: 3;
      color: #ffffff;
      text-align: center;
    }

    .card-content h1 {
      font-size: 2.5rem;
      margin: 0 0 10px 0;
      letter-spacing: 1px;
    }

    .card-content p {
      font-size: 1.1rem;
      margin: 0;
      color: #cccccc;
    }

    /* 7. Infinite Sliding Animation Engine (Calculated for 3 items) */
    @keyframes infiniteCardSlider {
      0% {
        opacity: 0;
        transform: translateX(100%); /* Slides into the card frame from the right side */
      }
      5% {
        opacity: 1;
        transform: translateX(0);    /* Locks perfectly dead-center */
      }
      33.33% {
        opacity: 1;
        transform: translateX(0);    /* Holds static for viewing window */
      }
      38.33% {
        opacity: 0;
        transform: translateX(-100%); /* Gracefully slides out to the left window boundary */
      }
      100% {
        opacity: 0;
        transform: translateX(-100%); /* Stays fully hidden while others loop */
      }
    }
  </style>
</head>
<body>

  <!-- The Single Self-Contained Card -->
  <div class="showcase-card">
    
    <!-- Sliding Image Layers -->
    <div class="card-slide img-1"></div>
    <div class="card-slide img-2"></div>
    <div class="card-slide img-3"></div>
    
    <!-- Darkened gradient visual layout shield -->
    <div class="card-overlay"></div>
    
    <!-- Overlaid Static Foreground Info -->
    <div class="card-content">
      <h1>MoviPilot</h1>
      <p>Discover something amazing today.</p>
    </div>

  </div>

</body>
</html>
